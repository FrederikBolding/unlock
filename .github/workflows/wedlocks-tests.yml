name: wedlocks-tests

on:
  push:
    paths: 
      - "wedlocks/**"
  pull_request:
    paths: 
      - "wedlocks/**"

jobs:
  run-tests:
    runs-on: ubuntu-latest
    env:
      DOCKER_BUILDKIT: 1
      BUILDKIT_PROGRESS: plain
      BLOCKTIME: 0
    steps:
      - uses: actions/checkout@v2
      - name: Running tests
        run: scripts/tests.sh wedlocks